#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <stdint.h>
#include "hw6.h"

char *trim(char *s)
{
  exit(1);
}

char *sos_translate(char *s)
{
  exit(1);
}

char *squeeze_spaces(char *s)
{
  exit(1);
}

char *sos_terminate(char *s)
{
  exit(1);
}

uint8_t *ascii_to_sos(char *s)
{
  exit(1);
}

unsigned char *sos_pack(uint8_t *is, int len, int *outlen)
{
  exit(1);
}
	    
uint8_t *sos_encode(char *message, int *outlen)
{
  exit(1);
}
